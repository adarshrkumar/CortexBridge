{
    "buildCommand": "npm run build",
    "functions": {
        "dist/index.js": {
            "maxDuration": 30
        }
    },
    "rewrites": [{ "source": "/(.*)", "destination": "/dist/index" }]
}
